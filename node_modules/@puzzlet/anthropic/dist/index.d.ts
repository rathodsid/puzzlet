import Anthropic from '@anthropic-ai/sdk';
import { IModelPlugin, IPluginAPI, AgentMark, DeserializeConfig, InferenceOptions, AgentMarkOutput, AgentMarkStreamOutput } from '@puzzlet/agentmark';

type MessageCreateParams = Anthropic.MessageCreateParams;
declare class AnthropicChatPlugin implements IModelPlugin {
    provider: string;
    apiKey: string | undefined;
    constructor();
    setApiKey(apiKey: string): void;
    serialize(completionParams: MessageCreateParams, name: string, api: IPluginAPI): string;
    deserialize(agentMark: AgentMark, api: IPluginAPI, config?: DeserializeConfig): Promise<MessageCreateParams>;
    runInference(agentMark: AgentMark, api: IPluginAPI, options?: InferenceOptions): Promise<AgentMarkOutput>;
    streamInference(agentMark: AgentMark, api: IPluginAPI, options?: InferenceOptions): Promise<AgentMarkStreamOutput<any>>;
}

export { AnthropicChatPlugin, AnthropicChatPlugin as default };
